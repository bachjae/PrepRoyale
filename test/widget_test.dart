import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Prep Royale requires Firebase initialization before the widget tree
    // can be pumped. Integration tests live in test/integration/.
    expect(true, isTrue);
  });
}
